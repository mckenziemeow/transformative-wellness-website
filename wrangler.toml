# Cloudflare Workers Static Assets — deploy from repo root
# Replaces the deprecated Cloudflare Pages approach.
name = "transformative-wellness"
compatibility_date = "2026-03-28"

[assets]
directory = "."
