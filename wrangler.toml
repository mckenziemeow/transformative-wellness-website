# Cloudflare Pages — manual deploy from repo root:
#   npx wrangler pages deploy . --project-name=transformative-wellness
# Use CLOUDFLARE_ACCOUNT_ID from `wrangler whoami` (must match the logged-in account).
name = "transformative-wellness"
compatibility_date = "2024-01-01"
